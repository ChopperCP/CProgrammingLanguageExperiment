#include <stdio.h> //导入头文件，以使用printf()

// argc 和 argv 虽然本实验没必要，但为了保持良好的编程习惯就加上了
int main(int argc, char *argv[])
{
    printf("%s", "Hello world!"); // Hello world!
    return 0;                     // POSIX规范
}